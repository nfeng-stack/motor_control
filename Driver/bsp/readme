bsp目录分为两层
1.第一层为板级外设层，主要用于存放整个应用系统所存在的硬件外设；
  这一层的驱动主要向应用层提供同步的驱动接口
  如果驱动接口中有阻塞式的操作应该将耗时的操作给板级驱动线程做
  如果驱动接口需要向应用层返回执行的结果的话应该提供对应的回调函数
  同时所有的中断函数都应该在对应的外设中实现
2.第二层是芯片自带的外设接口,主要用来给板级外设提供实际的操作能力
  对于所有向板级层提供的接口,都应该是被封装的,板级层不能直接调用SDK提供的接口。
  其中提供的接口需要具备灵活性,不应提供死板固定的初始化函数